'use strict';

/**
 * reading-list-entry router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::reading-list-entry.reading-list-entry');
