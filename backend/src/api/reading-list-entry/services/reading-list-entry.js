'use strict';

/**
 * reading-list-entry service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::reading-list-entry.reading-list-entry');
